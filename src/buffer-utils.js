export function readFloat32LE(buf, offset) {
  return buf.readFloatLE(offset);
}

export function readUint16LE(buf, offset) {
  return buf.readUInt16LE(offset);
}

export function readInt8(buf, offset) {
  return buf.readInt8(offset);
}